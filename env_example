# Resume Tailor Crew - Environment Configuration Template
# Copy this file to .env and fill in your actual values
# NEVER commit .env file with real secrets to version control

# Required: DeepSeek is the default LLM provider (src/config/settings.yaml -> llm.provider),
# used for all agent reasoning/tool-loop calls.
DEEPSEEK_API_KEY=sk-your-deepseek-api-key-here

# Required: used for strict structured-output calls (llm.structured_model: gpt-4o)
# and for resume/job embedding similarity (text-embedding-3-small).
OPENAI_API_KEY=sk-your-openai-api-key-here
