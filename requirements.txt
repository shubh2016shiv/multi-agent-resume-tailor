# Resume Tailor Crew - Core Dependencies
# Mirror of pyproject.toml for pip compatibility

# Core
crewai>=0.1.0
openai>=1.0.0
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
pyyaml>=6.0
structlog>=25.0.0

# Resilience
tenacity>=8.2.0
pybreaker>=1.0.0
aiobreaker>=0.5.0
ratelimit>=2.2.1

# Memory & Caching
redis>=5.0.0

# Document Processing (PDF, DOCX → Markdown for LLMs)
markitdown>=0.1.0

# Observability
langsmith>=0.1.0
litellm>=1.0.0        # Token counting + cost tracking across providers
